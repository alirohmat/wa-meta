# WABOT config example
# copy to .env and fill values

# required: key for /api/pair, /api/send, /api/generate, /api/logout
# client must send X-API-Key: <key>  or  Authorization: Bearer <key>  or  ?key=<key>
API_KEY=change-me-32-hex-random

BOT=867051314767696@bot
WEBHOOK=
PORT=8000
